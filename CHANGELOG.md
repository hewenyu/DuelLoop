# Changelog

## 0.2.2 — 宿主实时决策契约

- `decide` 支持逐调用 AbortSignal 和绝对 modelDeadline；模型取消不会误停其他 stream，实际模型失败仍停止。权限 deadline 不再被模型预算覆盖。
- `pinTrajectory` 支持首个决策前持久绑定策略；runtime 5 明确新的时间行为，旧发布需要重新绑定和验证。
- 回执支持稳定 eventId 与持久去重，冲突不能覆盖，重复旧事件不能降级终态；自定义 Store 的 recordReceipt 现在返回 boolean。
- ResearchWorker 可选择 first_settlement，仅新结算轨迹触发；修订仍保留在快照，默认继续使用 latest_revision。
- SQLite Schema 3 原子迁移，保留历史证据并补齐首次结算 ledger；共享同库进程必须一起升级。没有新增金额准入或真实模型调用。

## 0.2.1 — SDK 审计修复

- Pi 每次内部模型发送前复核持久研究状态，跨实例取消阻止工具续问与格式修复继续发送；保留已经发生的用量，禁用绕过检查的传输重试。
- 最终验证报告先原子进入 `validated_pending_release`，再原子登记发布与成功状态。`recover()` 与 Worker 可完成本地发布，兼容旧版成功但未登记的任务，不重复评估或消费保留集。
- `revise` 清除旧候选的最终资格，下一轮必须重新提交。研究结束后释放本轮 Pi 会话，等待迟到请求结算后再清理。
- Token 与费用完整性独立记录；`knownCostUsd` 保留小计，费用不全时 `costUnknown: true` 且不提供总额 `costUsd`。
- 增加真实 Pi 适配器的本地 HTTP 回归、发布事务故障及进程退出恢复测试。策略 2.0、评价协议 3.0、运行时 4 和 SQLite Schema 2 不变；升级前备份，共享同库进程应同时升级。

## 0.2.0 — 持续运行工程修复

- 研究产出与安全边界激活解耦，延期、候选失效和运行故障分别处理；同批反馈按持久事件游标触发一次。
- 统一决策、执行、激活、回退的使用资格，live 恢复不能继承 fixture 研究验证；模型行为指纹增加 kind、适配器、部署、协议及无凭据配置摘要。
- 直接嵌入请求和底层异步操作统一取消、排空；`start` 响应取消信号；异步订阅异常不终止运行。
- SQLite Schema 1→2 原子迁移；执行、发布、反馈及研究状态查询使用索引，快照采用有界窗口并缩短写锁时间。
- 付费研究前检查保留集资源，耗尽进入 `waiting_protocol`；保留集协议与额度持久冻结，不能更名复用种子。
- 评价协议升级至 `3.0`，性能门禁明确为 `maxP95DecisionComputeMs`；新增完整 SDK 分阶段遥测和 1k/10k/100k 历史并发基准。
- 运行时为 `duelloop-runtime-4`。策略 Schema 仍为 `2.0`，应用配置 Schema 仍为 `1.0`。自定义 DecisionModel 现在必须提供 behaviorIdentity；旧发布需要重新绑定和验证，历史证据与额度保留。

## 0.1.0 — 模型决策语义修订

- 策略 Schema 与评价协议升级为 `2.0`；移除置信度接受门槛、退出兜底、领域基线和降级指标，旧字段明确拒绝。
- 每个实际动作必须基于当次模型回答，包括单候选；合法的低置信度回答正常消费。模型失败记录 `stopped` 与 `stopReason`，抛错停止，不创建替代执行意图。
- 失败停止的运行实例不能用 `start()` 自动恢复；修复原因并核对在途外部执行后，显式重建实例。
- 运行时行为版本为 `duelloop-runtime-3`，旧发布与验证需重新绑定；SQLite Schema 仍为 1，历史证据保留。
- M0 仅比较 Jev Choice 和 Score，示例、领域模板、研究工具、基准与文档同步新契约。历史真实实验单独标记，不追溯视为新语义验证。

## 0.1.0 — 开发版本

- 建立公开 TypeScript SDK、运行时配置与领域契约。
- 实现声明式 Score 策略、语义校验、稳定摘要、组合评分和随机化选择。
- 实现托管/嵌入运行、统一执行检查、SQLite 意图/回执、轨迹绑定及激活资格检查。
- 接入锁定的 Jev 和 pi SDK，隔离研究工具与资源，支持单模型/团队、有界实验与取消。
- 提供领域评价、独立保留集预算、Kuhn/竞价模拟器及外部 SDK 演示。
- 提供 CLI、自动研究 worker、备份恢复、清理、打包消费测试和开发者文档。
- 严格核对独立消费者公开类型，直接执行安装包中的文档与模板示例。
- 补充发布阻塞诊断、反馈修订触发、备份只读检查、存储配额和重复并发基准。
- 采用 MIT 许可证，安装包包含完整许可文本。
- 增加真实服务实验入口、完整候选工具 Schema、pi 有界 JSON 格式修复和失败用量记账。
- Jev 在原有容差内归一化舍入概率；运行时行为版本升级为 `duelloop-runtime-2`，旧行为绑定需重新评价。

首版数据库 Schema 为 1。0.2.0 提供向 Schema 2 的增量迁移；真实服务验收及当前能力范围见 docs/validation.md。
